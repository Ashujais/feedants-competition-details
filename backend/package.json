{
  "name": "feedants-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "seed": "node scripts/seed.js",
    "test": "node --test",
    "check": "node --check src/app.js && node --check src/services/registrationService.js && node --check scripts/seed.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "feedants-competition-details": "file:..",
    "helmet": "^8.1.0",
    "mongoose": "^9.9.3",
    "morgan": "^1.10.1",
    "zod": "^4.1.12"
  }
}
